name: duel_ai
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
 
  google_fonts: ^6.2.1
  avatar_glow: ^3.0.1   # for glowing mic animation
  confetti: ^0.8.0
  simple_animations: ^5.2.0
  speech_to_text: ^7.3.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/shinchan.png
    - assets/cartoon.png
    - assets/cartoon2.png
